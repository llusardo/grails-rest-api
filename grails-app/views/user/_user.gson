import grails.rest.api.security.User

model {
    User user
}

json {
    id user.id
    username user.username
    authorities g.render(template:"/role/role", collection: user.getAuthorities(), var: 'role')
}